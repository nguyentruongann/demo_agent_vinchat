[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python -m src.backend.services.ingest_postgres --reset && sleep infinity"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3